# รายงานการตรวจสอบระบบเว็บไซต์ Rank1 City (System Audit Report)
**วันที่:** 2 ธันวาคม 2025
**ผู้ตรวจสอบ:** Antigravity (AI Senior Architect)
**เวอร์ชัน:** Next.js 14 (App Router)

---

## 1. 🚦 การตรวจสอบประสิทธิภาพ (Lighthouse Audit)

| หมวดหมู่ | คะแนน (ประเมิน) | สรุปผล |
| :--- | :--- | :--- |
| **Performance** | **75/100** | **ปัญหาสำคัญ:** ส่วน Hero (`LiveServerHero`) ถูกตั้งค่าเป็น `ssr: false` ทำให้การแสดงผลเนื้อหาหลัก (LCP) ล่าช้า และรูปภาพ Placeholder ยังดึงจากเว็บภายนอก |
| **Accessibility** | **90/100** | โครงสร้าง HTML ดีเยี่ยม รองรับ Screen Reader ได้ดี เมนูมือถือใช้งานได้ แต่ต้องตรวจสอบเรื่อง Focus Trap เพิ่มเติม |
| **SEO** | **85/100** | Metadata ครบถ้วน มี JSON-LD **ข้อควรระวัง:** แท็ก `<h1>` หลักอยู่ในส่วนที่ Render ฝั่ง Client ซึ่ง Google Index ได้แต่อาจไม่ดีเท่า Server-Side |
| **Best Practices** | **95/100** | ใช้ Tech Stack ทันสมัย มี Security Headers (CSP) ที่เข้มงวด และรองรับ HTTPS |

---

## 2. 🏗️ สถาปัตยกรรมและโค้ด (Architecture & Code)

### ✅ สิ่งที่ทำได้ดี
*   **App Router:** ใช้งานโครงสร้าง `app/` ได้ถูกต้องตามมาตรฐาน Next.js 14
*   **Server/Client Split:** แยกการทำงานได้ดี `app/page.js` ทำหน้าที่ดึงข้อมูล (Server) และส่งต่อให้ `HomeClient.js` (Client)
*   **Dynamic Imports:** ใช้ `next/dynamic` โหลด Component หนักๆ (เช่น FeatureTabs) เมื่อจำเป็น ช่วยลดขนาดไฟล์เริ่มต้น
*   **Configuration:** แยกการตั้งค่าไว้ใน `lib/config.json` ทำให้แก้ไขง่ายไม่ต้องแก้โค้ด

### ⚠️ สิ่งที่ต้องแก้ไข
*   **Anti-Pattern (Hero SSR):** ใน `components/HomeClient.js` มีการปิด SSR ของ `LiveServerHero` (`ssr: false`)
    *   *ผลกระทบ:* ผู้ใช้จะเห็นหน้าขาวหรือ Skeleton ก่อนที่เนื้อหาหลักจะมา ส่งผลเสียต่อ SEO และความรู้สึกผู้ใช้งาน
    *   *วิธีแก้:* เปลี่ยนเป็น `ssr: true` (Component นี้ปลอดภัยต่อ SSR เพราะโค้ดที่ใช้ `window` อยู่ใน Event Handler)

---

## 3. ⚡ ประสิทธิภาพเชิงลึก (Deep Performance)

### 🔍 Bundle Size & Loading
*   **Lazy Loading:** ใช้ `LazyMotion` ของ Framer Motion ได้ดีมาก ช่วยลดขนาดไฟล์ JS
*   **Image Optimization:** ใช้ `next/image` ถูกต้อง
*   **External Assets:** รูปภาพตัวอย่างยังดึงจาก `placehold.co`
    *   *ความเสี่ยง:* ถ้าเว็บฝากรูปช้า เว็บเราจะช้าไปด้วย
    *   *วิธีแก้:* ควรใช้รูปจริงหรือรูป Placeholder ที่เก็บไว้ในเครื่อง (`public/images/`)

### 🐢 Render Blocking
*   **Hydration:** หน้าแรกทั้งหมดถูกหุ้มด้วย `HomeClient` ทำให้ Navbar และ Footer ต้องรอโหลด JS ก่อนถึงจะแสดง
    *   *คำแนะนำ:* ควรย้าย `Navbar` และ `Footer` ไปไว้ใน `app/layout.js` เพื่อให้แสดงผลทันทีจาก Server ทุกหน้า

---

## 4. 🔒 ความปลอดภัย (Security Audit - OWASP)

| การตรวจสอบ | สถานะ | หมายเหตุ |
| :--- | :--- | :--- |
| **Injection** | ✅ **ผ่าน** | ไม่มีการเขียน SQL ตรงๆ ใน Client ข้อมูลถูกส่งผ่าน Props |
| **Sensitive Data** | ✅ **ผ่าน** | ไฟล์ Config และ Features ถูกอ่านที่ Server ไม่หลุดไปที่ Client |
| **Access Control** | ✅ **ผ่าน** | มีระบบ NextAuth และการเช็คสิทธิ์ Admin ใน Navbar |
| **Misconfiguration** | ✅ **ผ่าน** | **ดีเยี่ยม** มีการตั้งค่า Security Headers (CSP, HSTS) ใน `next.config.js` ครบถ้วน |
| **XSS** | ✅ **ผ่าน** | React ป้องกัน XSS โดยพื้นฐานอยู่แล้ว ส่วน JSON-LD ใช้ `dangerouslySetInnerHTML` อย่างระมัดระวัง |

---

## 5. 📱 การแสดงผลบนอุปกรณ์ต่างๆ (Responsiveness)

*   **Mobile Menu:** เมนูมือถือทำงานได้ดี มีปุ่มปิดและเปิด
*   **Footer:** จัด Layout ได้ดี (เรียงแนวตั้งในมือถือ แนวนอนในคอม)
*   **Hero Section:** ใช้ `min-h-[80vh]` ซึ่งดี แต่ในมือถือบางรุ่นอาจมีปัญหา Address Bar บัง
    *   *คำแนะนำ:* ใช้ `dvh` (Dynamic Viewport Height) ซึ่งในโค้ดมีการใช้บ้างแล้ว ถือว่าดี

---

## 6. 🎨 UI/UX (User Experience)

*   **Visual Hierarchy:** ปุ่ม "JOIN SERVER" เด่นชัดเจน เป็นจุดนำสายตาที่ดี
*   **Feedback:** มี Effect เมื่อเอาเมาส์ไปชี้ (Hover) ที่ปุ่มและการ์ดต่างๆ
*   **Light/Dark Mode:** รองรับทั้งสองโหมด
    *   *ตรวจสอบ:* เช็คความชัดเจนของตัวหนังสือในโหมดสว่าง (Light Mode) โดยเฉพาะส่วน Hero ที่มีพื้นหลังซ้อนกัน
*   **Loading States:** มี Skeleton Loading
    *   *ปรับปรุง:* Skeleton ยังเป็นกล่องสี่เหลี่ยมธรรมดา ควรปรับให้รูปร่างคล้ายเนื้อหาจริงมากขึ้น เพื่อลดความรู้สึกว่าหน้าเว็บกระตุก

---

## 7. 📝 สิ่งที่ต้องทำ (Action Plan)

ไฟล์ `todolist.md` ได้ถูกสร้างขึ้นแล้ว โดยมีรายการแก้ไขเร่งด่วนดังนี้:

1.  **[ด่วนที่สุด] เปิด SSR ให้ Hero Section:** แก้ไข `components/HomeClient.js` เปลี่ยน `ssr: false` เป็น `ssr: true`
2.  **[ด่วน] เปลี่ยนรูป Placeholder:** เอารูปจาก `placehold.co` ออก และใช้รูปจริง
3.  **[สำคัญ] ย้าย Navbar/Footer:** พิจารณาย้ายไป `layout.js` เพื่อประสิทธิภาพที่ดีขึ้น
4.  **[ทั่วไป] ตรวจสอบ Link:** แก้ไขลิงก์ Discord/Facebook ใน `config.json` ให้เป็นของจริง
