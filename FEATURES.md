# 📖 คู่มือระบบ Rank1 City - Pre-Registration Website

> เอกสารอธิบายฟีเจอร์ทั้งหมดของเว็บไซต์ลงทะเบียนล่วงหน้า Rank1 City FiveM Server

---

## 📋 สารบัญ

1. [ภาพรวมระบบ](#-ภาพรวมระบบ)
2. [ระบบยืนยันตัวตน (Authentication)](#-ระบบยืนยันตัวตน-authentication)
3. [ระบบลงทะเบียนล่วงหน้า (Pre-Registration)](#-ระบบลงทะเบียนล่วงหน้า-pre-registration)
4. [ระบบแก๊ง (Gang System)](#-ระบบแก๊ง-gang-system)
5. [ระบบครอบครัว (Family System)](#-ระบบครอบครัว-family-system)
6. [ระบบ Tier ตามจำนวนสมาชิก](#-ระบบ-tier-ตามจำนวนสมาชิก)
7. [ระบบเชิญและรับรางวัล (Invite & Earn)](#-ระบบเชิญและรับรางวัล-invite--earn)
8. [ระบบ Ticket/Giveaway](#-ระบบ-ticketgiveaway)
9. [การตั้งค่าและ Configuration](#-การตั้งค่าและ-configuration)

---

## 🌐 ภาพรวมระบบ

### เทคโนโลยีที่ใช้
| เทคโนโลยี | รายละเอียด |
|-----------|------------|
| **Framework** | Next.js 16 (App Router) |
| **Styling** | Tailwind CSS + shadcn/ui |
| **Database** | MySQL |
| **Authentication** | NextAuth.js (Discord OAuth) |
| **Animation** | Framer Motion |
| **Notification** | Sonner (Toast) |

### โครงสร้างโปรเจค
```
├── app/                    # Next.js App Router
│   ├── api/               # API Routes
│   │   ├── auth/          # Authentication API
│   │   ├── gang/          # Gang API
│   │   ├── family/        # Family API
│   │   └── preregister/   # Pre-registration API
│   ├── preregister/       # Pre-register Page
│   └── globals.css        # Global Styles
├── components/            # React Components
│   ├── ui/               # shadcn/ui Components
│   ├── GangManager.js    # Gang Management Component
│   ├── FamilyManager.js  # Family Management Component
│   └── InviteEarn.js     # Invite System Component
├── database/             # SQL Scripts
└── lib/                  # Utilities & Database Connection
```

---

## 🔐 ระบบยืนยันตัวตน (Authentication)

### การเข้าสู่ระบบด้วย Discord OAuth
- ผู้ใช้สามารถเข้าสู่ระบบผ่าน Discord Account
- ระบบจะดึงข้อมูล:
  - Discord ID
  - Discord Username
  - Avatar URL
- ใช้ NextAuth.js เป็น Provider

### การจัดการ Session
```javascript
// ตรวจสอบ Session
const { data: session } = useSession();

// ข้อมูลที่ได้รับ
session.user = {
  id: "Discord ID",
  name: "Username",
  image: "Avatar URL"
}
```

---

## 📝 ระบบลงทะเบียนล่วงหน้า (Pre-Registration)

### ขั้นตอนการลงทะเบียน
1. **เข้าสู่ระบบด้วย Discord** - ยืนยันตัวตนผ่าน Discord OAuth
2. **กรอกข้อมูล** - ชื่อ-นามสกุลตัวละคร
3. **ยืนยันการลงทะเบียน** - บันทึกข้อมูลลง Database

### ข้อมูลที่จัดเก็บ
| Field | ประเภท | คำอธิบาย |
|-------|--------|----------|
| `discord_id` | VARCHAR(50) | รหัส Discord (Primary Key) |
| `discord_name` | VARCHAR(100) | ชื่อ Discord |
| `firstname` | VARCHAR(50) | ชื่อตัวละคร |
| `lastname` | VARCHAR(50) | นามสกุลตัวละคร |
| `avatar_url` | TEXT | URL รูป Avatar |
| `referral_code` | VARCHAR(20) | รหัสเชิญของตัวเอง |
| `referred_by` | VARCHAR(20) | รหัสเชิญที่ใช้ลงทะเบียน |
| `invite_count` | INT | จำนวนคนที่เชิญสำเร็จ |
| `ticket_count` | INT | จำนวน Ticket ที่ได้รับ |

---

## ⚔️ ระบบแก๊ง (Gang System)

### ฟีเจอร์หลัก

#### 1. สร้างแก๊ง
- **ชื่อแก๊ง**: 3-20 ตัวอักษร
- **โลโก้**: URL รูปภาพ (รองรับ pic.in.th, imgur, imgbb)
- **รหัสเชิญ**: สร้างอัตโนมัติ (รูปแบบ: `GANG-XXXXXX`)

#### 2. เข้าร่วมแก๊ง
- ใช้รหัสเชิญจากหัวหน้าแก๊ง
- สมาชิกสูงสุด: 25 คน

#### 3. จัดการสมาชิก (สำหรับหัวหน้า)
- ดูรายชื่อสมาชิกทั้งหมด
- เตะสมาชิกออกจากแก๊ง
- ดูข้อมูลสมาชิก (Discord Name, วันที่เข้าร่วม)

#### 4. ตั้งค่าแก๊ง (สำหรับหัวหน้า)
- เปลี่ยนชื่อแก๊ง
- เปลี่ยนโลโก้แก๊ง
- ตั้งประกาศ (MOTD)
- ยุบแก๊ง

#### 5. ความคืบหน้ารางวัล
- แสดง Progress Bar แนวนอน
- Milestone ที่ 10, 15, 20, 25 สมาชิก
- สีเปลี่ยนตาม Tier ที่ปลดล็อก
- Tooltip แสดงสถานะเมื่อ hover

### API Endpoints
| Method | Endpoint | Action |
|--------|----------|--------|
| GET | `/api/gang` | ดึงข้อมูลแก๊งของผู้ใช้ |
| POST | `/api/gang` | สร้าง/จัดการแก๊ง |
| GET | `/api/gang/members` | ดึงรายชื่อสมาชิก |

### Actions ที่รองรับ
```javascript
// สร้างแก๊ง
{ action: 'create', name: 'ชื่อแก๊ง', logoUrl: 'URL' }

// เข้าร่วมแก๊ง
{ action: 'join', inviteCode: 'GANG-XXXXXX' }

// อัพเดทโลโก้
{ action: 'update_logo', logoUrl: 'URL ใหม่' }

// อัพเดทการตั้งค่า
{ action: 'update_settings', name: 'ชื่อใหม่', motd: 'ประกาศ' }

// เตะสมาชิก
{ action: 'kick_member', targetDiscordId: 'Discord ID' }

// ออกจากแก๊ง
{ action: 'leave' }

// ยุบแก๊ง
{ action: 'dissolve' }
```

---

## 👨‍👩‍👧‍👦 ระบบครอบครัว (Family System)

### ฟีเจอร์หลัก
เหมือนกับระบบแก๊ง แต่ใช้ธีมและข้อความที่เหมาะกับครอบครัว

#### ความแตกต่างจากแก๊ง
| รายการ | แก๊ง | ครอบครัว |
|--------|------|----------|
| รหัสเชิญ | `GANG-XXXXXX` | `FAM-XXXXXX` |
| ไอคอนหัวหน้า | 👑 มงกุฎ (สีทอง) | ⭐ ดาว (สีชมพู) |
| ชื่อ Tier | ก่อร่าง, เติบโต, อิทธิพล, ตำนาน | รากฐาน, เติบโต, มั่นคง, มหาตระกูล |

### API Endpoints
| Method | Endpoint | Action |
|--------|----------|--------|
| GET | `/api/family` | ดึงข้อมูลครอบครัวของผู้ใช้ |
| POST | `/api/family` | สร้าง/จัดการครอบครัว |
| GET | `/api/family/members` | ดึงรายชื่อสมาชิก |

---

## 🎨 ระบบ Tier ตามจำนวนสมาชิก

### ระดับ Tier และสี

#### สำหรับแก๊ง
| สมาชิก | ชื่อ Tier | สี | Gradient |
|--------|----------|-----|----------|
| 1-9 | เริ่มต้น | 🔵 น้ำเงิน | `from-blue-600 to-indigo-600` |
| 10-14 | ก่อร่าง | 🟢 เขียว | `from-emerald-500 to-green-600` |
| 15-19 | เติบโต | 🟣 ม่วง | `from-purple-500 to-violet-600` |
| 20-24 | อิทธิพล | 🟠 ส้ม | `from-amber-500 to-orange-600` |
| 25+ | ตำนาน | 🔴 แดง | `from-red-500 to-rose-600` |

#### สำหรับครอบครัว
| สมาชิก | ชื่อ Tier | สี | Gradient |
|--------|----------|-----|----------|
| 1-9 | เริ่มต้น | 🔵 น้ำเงิน | `from-blue-600 to-indigo-600` |
| 10-14 | รากฐาน | 🟢 เขียว | `from-emerald-500 to-green-600` |
| 15-19 | เติบโต | 🟣 ม่วง | `from-purple-500 to-violet-600` |
| 20-24 | มั่นคง | 🟠 ส้ม | `from-amber-500 to-orange-600` |
| 25+ | มหาตระกูล | 🔴 แดง | `from-red-500 to-rose-600` |

### องค์ประกอบที่เปลี่ยนตาม Tier
1. **กรอบโลโก้** - Gradient border เปลี่ยนสี
2. **เงา (Shadow)** - สีเงาเรืองแสงเปลี่ยนตาม tier
3. **Badge ใต้โลโก้** - แสดงชื่อ Tier พร้อมสี gradient
4. **Progress Bar** - สีแถบ progress เปลี่ยนตาม tier
5. **Milestone Dots** - จุดบน progress bar มีสีตาม tier ที่ปลดล็อก

### โค้ดการคำนวณ Tier
```javascript
const getGangTierTheme = (memberCount) => {
    if (memberCount >= 25) return { tierName: 'ตำนาน', ... };
    if (memberCount >= 20) return { tierName: 'อิทธิพล', ... };
    if (memberCount >= 15) return { tierName: 'เติบโต', ... };
    if (memberCount >= 10) return { tierName: 'ก่อร่าง', ... };
    return { tierName: 'เริ่มต้น', ... };
};
```

---

## 🎁 ระบบเชิญและรับรางวัล (Invite & Earn)

### วิธีการทำงาน
1. ผู้ใช้ได้รับ **Referral Code** เฉพาะตัว
2. แชร์รหัสให้เพื่อน
3. เมื่อเพื่อนลงทะเบียนด้วยรหัส → ได้รับ **Ticket**
4. Ticket สามารถใช้ลุ้นรางวัลได้

### รางวัลตามจำนวนเชิญ
| จำนวนเชิญ | Ticket ที่ได้ |
|-----------|---------------|
| 1 คน | 1 Ticket |
| 5 คน | Bonus +2 Tickets |
| 10 คน | Bonus +5 Tickets |
| 25 คน | Bonus +10 Tickets |

### API Endpoint
```javascript
// ดึงข้อมูล Referral
GET /api/preregister

// Response
{
  referralCode: "REF-XXXXXX",
  inviteCount: 5,
  ticketCount: 7,
  referredUsers: [...]
}
```

---

## 🎟️ ระบบ Ticket/Giveaway

### การได้รับ Ticket
1. **เชิญเพื่อน** - ได้ 1 ticket ต่อ 1 คนที่เชิญสำเร็จ
2. **Bonus Milestone** - ได้ bonus เพิ่มเมื่อถึงเป้าหมาย
3. **กิจกรรมพิเศษ** - อาจได้รับจากกิจกรรมอื่นๆ

### การใช้งาน Ticket
- ใช้ลุ้นรางวัล Giveaway ในเซิร์ฟเวอร์
- จำนวน Ticket = โอกาสในการถูกรางวัล

---

## ⚙️ การตั้งค่าและ Configuration

### Environment Variables (.env)
```env
# Database
DATABASE_HOST=localhost
DATABASE_USER=root
DATABASE_PASSWORD=password
DATABASE_NAME=rank1city

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key

# Discord OAuth
DISCORD_CLIENT_ID=your-client-id
DISCORD_CLIENT_SECRET=your-client-secret

# Discord Webhook (สำหรับแจ้งเตือน)
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...
```

### Next.js Config (next.config.js)

#### Image Domains ที่รองรับ
```javascript
images: {
    remotePatterns: [
        { hostname: 'placehold.co' },
        { hostname: 'cdn.discordapp.com' },
        { hostname: 'raw.githubusercontent.com' },
        { hostname: 'images.unsplash.com' },
        { hostname: '**.pic.in.th' },      // Thai image hosting
        { hostname: 'i.imgur.com' },        // Imgur
        { hostname: '**.imgbb.com' },       // ImgBB
    ]
}
```

#### Content Security Policy (CSP)
```javascript
img-src: 'self' blob: data: 
         https://*.pic.in.th 
         https://i.imgur.com 
         https://*.imgbb.com 
         https://placehold.co 
         https://cdn.discordapp.com 
         https://raw.githubusercontent.com 
         https://images.unsplash.com
```

---

## 📊 Database Schema

### ตาราง `preregistrations`
```sql
CREATE TABLE preregistrations (
    discord_id VARCHAR(50) PRIMARY KEY,
    discord_name VARCHAR(100) NOT NULL,
    firstname VARCHAR(50),
    lastname VARCHAR(50),
    avatar_url TEXT,
    referral_code VARCHAR(20) UNIQUE,
    referred_by VARCHAR(20),
    invite_count INT DEFAULT 0,
    ticket_count INT DEFAULT 0,
    gang_code VARCHAR(20),
    family_code VARCHAR(20),
    ip_address VARCHAR(45),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### ตาราง `gangs`
```sql
CREATE TABLE gangs (
    gang_code VARCHAR(20) PRIMARY KEY,
    name VARCHAR(50) NOT NULL,
    logo_url TEXT,
    invite_code VARCHAR(20) UNIQUE,
    leader_discord_id VARCHAR(50),
    motd TEXT,
    max_members INT DEFAULT 25,
    level INT DEFAULT 1,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### ตาราง `families`
```sql
CREATE TABLE families (
    family_code VARCHAR(20) PRIMARY KEY,
    name VARCHAR(50) NOT NULL,
    logo_url TEXT,
    invite_code VARCHAR(20) UNIQUE,
    leader_discord_id VARCHAR(50),
    motd TEXT,
    max_members INT DEFAULT 25,
    level INT DEFAULT 1,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## 🔄 Workflow: Git Push & Discord Notify

### การใช้งาน
```bash
# ใช้คำสั่ง /git_push_notify
```

### ขั้นตอน
1. `git status` - ตรวจสอบการเปลี่ยนแปลง
2. `git add .` - เพิ่มไฟล์ทั้งหมด
3. `git commit -m "message"` - Commit พร้อมข้อความ
4. `git push` - Push ขึ้น Repository
5. `node .agent/workflows/notify_discord.js` - ส่งแจ้งเตือน Discord

---

## 📝 หมายเหตุ

### การอัพเดทในอนาคต
- [ ] ระบบ Leaderboard สำหรับแก๊ง/ครอบครัว
- [ ] ระบบ Achievement
- [ ] ระบบ Chat ภายในแก๊ง/ครอบครัว
- [ ] ระบบ War ระหว่างแก๊ง
- [ ] ระบบ Alliance ระหว่างครอบครัว

### Contact
- **Discord Server**: [Rank1 City]
- **GitHub**: TRPxx/rank1-city-web

---

> 📅 อัพเดทล่าสุด: 6 ธันวาคม 2567
> 
> ✍️ เขียนโดย: Antigravity AI Assistant
