# 🏙️ Rank1 City - System Architecture & Technical Summary

เอกสารฉบับนี้สรุปภาพรวมเชิงเทคนิค สถาปัตยกรรม และระบบการทำงานทั้งหมดของเว็บไซต์ **Rank1 City** เพื่อใช้สำหรับทีมพัฒนา (Developers) และผู้ดูแลระบบ (Admins)

---

## 1. 🏗️ สถาปัตยกรรมระบบ (System Architecture)

### **Tech Stack**
*   **Frontend Framework**: Next.js 14 (App Router) - ใช้ฟีเจอร์ Server Components และ Client Components ผสมผสานกัน
*   **Language**: JavaScript (ES6+)
*   **Styling**: Tailwind CSS + Shadcn UI (Radix UI base)
*   **Animation**: Framer Motion (LazyMotion implementation)
*   **State Management**: React Hooks (`useState`, `useEffect`, `useContext`) + Custom Hooks
*   **Database**: MySQL (เชื่อมต่อผ่าน `mysql2` library พร้อม Connection Pooling)
*   **Authentication**: NextAuth.js (Discord Provider)
*   **Testing**: Jest + React Testing Library

### **Design Patterns**
*   **Component-Based**: แยก UI เป็นชิ้นส่วนย่อย (Components) ที่นำกลับมาใช้ซ้ำได้ (Reusable)
*   **Hook-Based Logic**: แยก Business Logic ออกจาก UI โดยใช้ Custom Hooks (เช่น `useLuckyDraw`, `usePreregisterStatus`)
*   **Server-Side Rendering (SSR) & Static Site Generation (SSG)**: ใช้ประโยชน์จาก Next.js เพื่อ SEO และ Performance ที่ดีที่สุด

---

## 2. 📦 โมดูลและฟีเจอร์หลัก (Key Modules)

### **2.1 ระบบยืนยันตัวตน (Authentication)**
*   **Provider**: Discord OAuth2
*   **Implementation**: ใช้ `NextAuth.js` จัดการ Session และ Callback
*   **Flow**:
    1. ผู้ใช้กดปุ่ม "ลงทะเบียนล่วงหน้า"
    2. Redirect ไปยังหน้า Login ของ Discord
    3. เมื่อ Login สำเร็จ ระบบจะตรวจสอบข้อมูลใน Database
    4. หากเป็นผู้ใช้ใหม่ -> สร้าง Record ใหม่ในตาราง `users`
    5. หากเป็นผู้ใช้เก่า -> อัปเดตข้อมูลล่าสุด (Avatar, Username)

### **2.2 ระบบลงทะเบียนล่วงหน้า (Pre-registration Dashboard)**
*   **Invite System**:
    *   ผู้ใช้ทุกคนจะมี `myCode` (Referral Code)
    *   สามารถนำโค้ดไปชวนเพื่อนได้
    *   ระบบจะนับจำนวน Invite และคำนวณของรางวัลตาม Tier
*   **Gang System**:
    *   หัวหน้าแก๊งสามารถสร้างแก๊งและรับ `Token`
    *   ลูกทีมนำ Token มากรอกเพื่อเข้าร่วมแก๊ง
    *   แสดงรายชื่อสมาชิกแก๊งแบบ Real-time
*   **Lucky Draw (Gachapon)**:
    *   **Logic**: คำนวณการสุ่มที่ฝั่ง Server (API Route) เพื่อป้องกันการโกง
    *   **Animation**: ใช้ `useLuckyDraw` hook ควบคุม Animation การหมุนแบบ Tape-roll
    *   **Data**: เก็บประวัติการสุ่มลง Database (`lucky_draw_history`)

### **2.3 ระบบแสดงผลหน้าแรก (Landing Page)**
*   **Hero Section**: วิดีโอพื้นหลังและข้อความต้อนรับ
*   **Global Counter**: แสดงยอดผู้ลงทะเบียนรวมทั้งเซิร์ฟเวอร์ (Real-time polling ทุก 30 วินาที)
*   **Feature Showcase**: แสดงระบบเด่นของเซิร์ฟเวอร์ด้วย Tab Interface
*   **News & Updates**: แสดงข่าวสารล่าสุด

---

## 3. 💾 ฐานข้อมูล (Database Schema)

ระบบใช้ MySQL โดยมีตารางหลักดังนี้:

### **`users`**
เก็บข้อมูลผู้เล่น
*   `discord_id` (PK): ไอดี Discord
*   `name`: ชื่อผู้ใช้
*   `image`: รูปโปรไฟล์
*   `invite_code`: โค้ดแนะนำเพื่อน
*   `invited_by`: ไอดีของผู้ที่แนะนำมา
*   `ticket_count`: จำนวนตั๋วสุ่มกาชา

### **`gangs`**
เก็บข้อมูลแก๊ง
*   `id`: รหัสแก๊ง
*   `name`: ชื่อแก๊ง
*   `leader_id`: Discord ID ของหัวหน้าแก๊ง
*   `token`: รหัสเข้าร่วมแก๊ง

### **`lucky_draw_history`**
เก็บประวัติการสุ่ม
*   `id`: Auto Increment
*   `user_id`: Discord ID ผู้สุ่ม
*   `reward_name`: ชื่อของรางวัล
*   `rarity`: ระดับความหายาก
*   `created_at`: เวลาที่สุ่ม

---

## 4. 🚀 การปรับปรุงประสิทธิภาพ (Performance Optimization)

*   **Lazy Motion**: ใช้ `m` component จาก `framer-motion` และโหลดฟีเจอร์ Animation เฉพาะตอนที่จำเป็น ช่วยลด Bundle Size
*   **Image Optimization**: ใช้ `next/image` แปลงรูปเป็น WebP และปรับขนาดอัตโนมัติ
*   **AbortController**: ยกเลิก Request ที่ค้างอยู่เมื่อผู้ใช้เปลี่ยนหน้า ป้องกัน Memory Leak
*   **Database Pooling**: ใช้ `mysql2` connection pool เพื่อรองรับ Concurrent users จำนวนมาก โดยมีการจัดการ Error `PROTOCOL_CONNECTION_LOST`

---

## 5. 🛡️ ความปลอดภัย (Security)

*   **API Route Protection**: ตรวจสอบ Session (`getServerSession`) ทุกครั้งก่อนทำรายการสำคัญ (เช่น สุ่มรางวัล, สร้างแก๊ง)
*   **Parameterized Queries**: ใช้ `?` ใน SQL Query เพื่อป้องกัน SQL Injection 100%
*   **Environment Variables**: เก็บข้อมูลความลับ (DB Password, API Keys) ในไฟล์ `.env` ไม่มีการ Hardcode
*   **Log Sanitization**: ลบ `console.log` ที่แสดงข้อมูล Sensitive ออกจาก Production code

---

## 6. 🧪 การทดสอบ (Testing)

*   **Framework**: Jest + React Testing Library
*   **Coverage**:
    *   **Unit Tests**: ทดสอบ Logic ของ Hooks (`useLuckyDraw`) และ Components (`GlobalRegisCounter`)
    *   **Integration Tests**: (Planned) ทดสอบ API Routes และ Database interaction

---

## 7. 📂 โครงสร้างไฟล์สำคัญ (Key Files)

*   `app/api/preregister/route.js`: API หลักสำหรับจัดการข้อมูลผู้ใช้และการลงทะเบียน
*   `components/HomeClient.js`: หน้าหลักฝั่ง Client ควบคุมการแสดงผลและ Animation
*   `hooks/useLuckyDraw.js`: Logic ทั้งหมดของระบบสุ่มรางวัล
*   `hooks/usePreregisterStatus.js`: Logic การดึงข้อมูลสถานะผู้ใช้
*   `lib/db.js`: การตั้งค่าเชื่อมต่อฐานข้อมูล
*   `lib/preregister-config.js`: การตั้งค่าของรางวัลและเรทการสุ่ม (Config file)

---

*เอกสารฉบับนี้จัดทำขึ้นเมื่อ 30 พฤศจิกายน 2025 โดยทีมงาน Rank1 Development*
