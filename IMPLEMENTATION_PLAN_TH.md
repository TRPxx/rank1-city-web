# แผนการแก้ไขและปรับปรุงระบบ (Implementation Plan)

แผนงานนี้เรียงลำดับตามความสำคัญและผลกระทบต่อระบบ เพื่อให้การแก้ไขเป็นไปอย่างราบรื่นและมีประสิทธิภาพสูงสุด

---

## 🚨 ระยะที่ 1: แก้ไขปัญหาเร่งด่วน (Critical Fixes)
**เป้าหมาย:** แก้ไขปัญหาที่ส่งผลกระทบต่อ SEO, ประสิทธิภาพการโหลด (LCP), และประสบการณ์ผู้ใช้โดยตรง

1.  **เปิดใช้งาน SSR สำหรับ Hero Section**
    *   **ไฟล์:** `components/HomeClient.js`
    *   **การกระทำ:** เปลี่ยนการตั้งค่า `dynamic import` ของ `LiveServerHero` จาก `ssr: false` เป็น `ssr: true`
    *   **เหตุผล:** เพื่อให้ Google Bot มองเห็นเนื้อหาหลัก (H1) และลดเวลา LCP (Largest Contentful Paint)

2.  **ปรับปรุงโครงสร้าง Layout (Navbar & Footer)**
    *   **ไฟล์:** `app/layout.js`, `app/page.js`, `components/HomeClient.js`
    *   **การกระทำ:** ย้าย `<Navbar />` และ `<Footer />` ออกจาก `HomeClient` ไปไว้ใน `RootLayout` (`app/layout.js`)
    *   **เหตุผล:** เพื่อให้เมนูและฟุตเตอร์แสดงผลทันทีทุกหน้า ไม่ต้องรอ JS โหลด และลด Layout Shift

3.  **แก้ไขรูปภาพ Placeholder**
    *   **ไฟล์:** `lib/config.json`
    *   **การกระทำ:** ดาวน์โหลดรูปภาพตัวอย่างมาเก็บไว้ใน `public/images/` และเปลี่ยนลิงก์ใน Config ให้ชี้ไปที่ไฟล์ในเครื่องแทน `placehold.co`
    *   **เหตุผล:** ลดการพึ่งพาเว็บภายนอก เพิ่มความเร็วในการโหลดรูป

---

## 🛠️ ระยะที่ 2: ปรับปรุงคุณภาพโค้ดและ UX (Code Quality & UX)
**เป้าหมาย:** เพิ่มความสมบูรณ์ของระบบ ลดบั๊ก และปรับปรุงการใช้งาน

4.  **สร้าง Skeleton Loading ที่สมจริง**
    *   **ไฟล์:** สร้าง `components/skeletons/HeroSkeleton.js`
    *   **การกระทำ:** สร้าง Skeleton ที่มีรูปร่างคล้ายกับ Hero Section จริงๆ (มีก้อนข้อความ, ปุ่ม) แทนกล่องสี่เหลี่ยมโง่ๆ
    *   **เหตุผล:** ลดความรู้สึกว่าเว็บ "กระตุก" หรือ "โหลดช้า" (Perceived Performance)

5.  **ตรวจสอบและแก้ไข Link ภายนอก**
    *   **ไฟล์:** `lib/config.json`
    *   **การกระทำ:** แทนที่ลิงก์ชั่วคราว (เช่น `discord.gg/your-invite`) ด้วยลิงก์จริง หรือใส่ `#` ไว้ก่อนถ้ายังไม่มี
    *   **เหตุผล:** ป้องกันผู้ใช้กดแล้วไปเจอหน้า Error

6.  **ลบ Console Log ใน Production**
    *   **ไฟล์:** `components/HomeClient.js`
    *   **การกระทำ:** ลบหรือ comment out `console.log` ที่ไม่จำเป็น
    *   **เหตุผล:** ทำให้ Console สะอาดและดูเป็นมืออาชีพ

---

## 🔒 ระยะที่ 3: ความปลอดภัยและ SEO ขั้นสูง (Security & Advanced SEO)
**เป้าหมาย:** เสริมความปลอดภัยและทำให้เว็บติดอันดับได้ดีขึ้น

7.  **Generate Sitemap & Robots.txt**
    *   **ไฟล์:** สร้าง `app/sitemap.js` และ `app/robots.js`
    *   **การกระทำ:** ใช้ฟีเจอร์ของ Next.js ในการสร้างไฟล์เหล่านี้อัตโนมัติ
    *   **เหตุผล:** ช่วยให้ Google Index หน้าเว็บได้ครบถ้วนและถูกต้อง

8.  **เพิ่ม Alt Text ให้ครบถ้วน**
    *   **ไฟล์:** `components/Footer.js`, `components/FeatureTabs.js`
    *   **การกระทำ:** ไล่เช็ค `Image` ทุกจุดว่ามี `alt` ที่สื่อความหมายหรือไม่
    *   **เหตุผล:** ดีต่อ SEO และผู้พิการทางสายตา (Accessibility)

9.  **ตรวจสอบ NextAuth Secret**
    *   **ไฟล์:** `.env`
    *   **การกระทำ:** ตรวจสอบว่ามี `NEXTAUTH_SECRET` ที่สุ่มมาอย่างปลอดภัยหรือไม่
    *   **เหตุผล:** ป้องกันการปลอมแปลง Session

---

## 📅 ตารางเวลาโดยประมาณ

| ระยะ | งาน | เวลาที่ใช้ (ประมาณ) |
| :--- | :--- | :--- |
| **ระยะที่ 1** | SSR Fix, Layout Move, Image Fix | 1 - 2 ชั่วโมง |
| **ระยะที่ 2** | Skeletons, Link Fix, Cleanup | 2 - 3 ชั่วโมง |
| **ระยะที่ 3** | Sitemap, Robots, Alt Text, Security | 1 - 2 ชั่วโมง |

**รวมเวลาทั้งหมด:** ประมาณ 4 - 7 ชั่วโมง (ขึ้นอยู่กับความซับซ้อนหน้างาน)
