# 🚀 คู่มือการติดตั้งเว็บไซต์บน DDC Server (Windows)

คู่มือนี้จะสอนวิธีนำเว็บไซต์ Next.js ไปรันบน Windows Server แบบ Production พร้อมระบบ Auto-restart เมื่อเครื่องดับ

---

## 🛠️ 1. เตรียมเครื่อง Server (Prerequisites)
สิ่งที่ต้องติดตั้งลงในเครื่อง Server ก่อนเริ่มงาน:

1.  **Node.js (LTS Version)**
    *   ดาวน์โหลด: [https://nodejs.org/](https://nodejs.org/) (เลือกเวอร์ชัน LTS)
    *   ติดตั้งแล้วเช็คเวอร์ชัน: เปิด CMD พิมพ์ `node -v`

2.  **Git**
    *   ดาวน์โหลด: [https://git-scm.com/download/win](https://git-scm.com/download/win)
    *   ติดตั้งแล้วเช็ค: `git --version`

3.  **PM2 (ตัวช่วยรันเว็บไซต์)**
    *   เปิด CMD (Run as Administrator) แล้วพิมพ์:
        ```bash
        npm install -g pm2
        ```

---

## 📥 2. ดึงโค้ดลง Server
1.  สร้างโฟลเดอร์สำหรับเก็บเว็บ (เช่น `C:\Websites`)
2.  เปิด CMD ในโฟลเดอร์นั้น
3.  ดึงโค้ดจาก GitHub:
    ```bash
    git clone https://github.com/TRPxx/rank1-city-web.git
    ```
4.  เข้าไปในโฟลเดอร์:
    ```bash
    cd rank1-city-web
    ```

---

## ⚙️ 3. ตั้งค่า (Configuration)
1.  **ติดตั้ง Library:**
    ```bash
    npm install
    ```
2.  **สร้างไฟล์ `.env`:**
    *   สร้างไฟล์ชื่อ `.env` ในโฟลเดอร์โปรเจกต์
    *   ก๊อปปี้ข้อมูลจากไฟล์ `.env` ในเครื่องเราไปใส่
    *   **⚠️ จุดที่ต้องแก้:**
        *   `DB_HOST`: ถ้า Database อยู่เครื่องเดียวกันใช้ `localhost`
        *   `DB_PASSWORD`: ใส่รหัสผ่าน Database ของ Server
        *   `NEXTAUTH_URL`: เปลี่ยนเป็น `http://IP_SERVER:3000` หรือ `https://yourdomain.com`

---

## 🏗️ 4. Build เว็บไซต์
แปลงโค้ดให้เป็นเวอร์ชันพร้อมใช้งาน (Production Build):
```bash
npm run build
```
*(ขั้นตอนนี้อาจใช้เวลา 1-2 นาที และต้องไม่มี Error สีแดง)*

---

## 🚀 5. รันเว็บไซต์ (Start Server)
เราจะใช้ **PM2** เพื่อให้เว็บทำงานตลอดเวลา

### ทางเลือกที่ 1: รัน Port 3000 (ค่าปกติ)
```bash
pm2 start npm --name "rank1-web" -- start
```

### ทางเลือกที่ 2: รัน Port 80 (ไม่ต้องพิมพ์เลข Port เวลาเข้าเว็บ)
ถ้าต้องการให้เข้าเว็บผ่าน `http://IP_SERVER` ได้เลยโดยไม่ต้องเติม `:3000` ให้ใช้คำสั่งนี้:
```bash
pm2 start npm --name "rank1-web" -- start -- -p 80
```
*(หมายเหตุ: การรัน Port 80 อาจต้องปิด IIS หรือ Service อื่นที่แย่งใช้ Port นี้ก่อน)*

---

## 💾 บันทึกสถานะ (Auto-start)
หลังจากรันคำสั่งด้านบนเสร็จแล้ว ให้พิมพ์คำสั่งนี้เพื่อให้เว็บเปิดเองตอนรีเครื่อง:
```bash
pm2 save
```

---

## 🌐 6. เปิด Port (Firewall)
ต้องเปิด Port ที่เราเลือกใช้ (3000 หรือ 80)

1.  กดปุ่ม Start พิมพ์ **"Windows Defender Firewall with Advanced Security"**
2.  คลิก **Inbound Rules** (ซ้ายมือ) -> **New Rule...** (ขวามือ)
3.  เลือก **Port** -> Next
4.  เลือก **TCP** และช่อง Specific local ports ใส่ **80** (หรือ 3000 ตามที่เลือก) -> Next
5.  เลือก **Allow the connection** -> Next -> Next
6.  ตั้งชื่อ: `NextJS Web Port 80` -> Finish

---

## 🔄 วิธีอัปเดตเว็บไซต์ (เมื่อมีการแก้โค้ด)
เมื่อคุณแก้โค้ดในเครื่องตัวเองและ Push ขึ้น GitHub แล้ว ให้ทำดังนี้ที่ Server:

1.  เข้าโฟลเดอร์โปรเจกต์: `cd C:\Websites\rank1-city-web`
2.  ดึงโค้ดใหม่: `git pull`
3.  Build ใหม่: `npm run build`
4.  รีสตาร์ทเว็บ: `pm2 restart rank1-web`

---

### ✅ เสร็จสิ้น!
ตอนนี้เว็บไซต์ของคุณพร้อมใช้งานแล้วที่ `http://IP_SERVER:3000`
