# 🏙️ Rank1 City - Ultimate FiveM Roleplay Website

![Rank1 City Banner](/public/images/hero-bg-fivem.webp)

**Rank1 City Website** คือแพลตฟอร์มเว็บแอปพลิเคชันครบวงจรสำหรับเซิร์ฟเวอร์ FiveM Roleplay ระดับพรีเมียม พัฒนาด้วยเทคโนโลยีที่ทันสมัยที่สุดเพื่อให้มั่นใจในประสิทธิภาพ ความสวยงาม และประสบการณ์ผู้ใช้ที่ดีเยี่ยม

โปรเจกต์นี้ไม่ได้เป็นเพียงแค่หน้า Landing Page แต่เป็น **Web Application** เต็มรูปแบบที่เชื่อมต่อกับฐานข้อมูลเกม รองรับระบบสมาชิก กิจกรรม และการจัดการภายในเซิร์ฟเวอร์

---

## 📑 สารบัญ

- [ฟีเจอร์และระบบหลัก (Key Features)](#-ฟีเจอร์และระบบหลัก-key-features)
- [โครงสร้างทางเทคนิค (Technical Architecture)](#-โครงสร้างทางเทคนิค-technical-architecture)
- [โครงสร้างโปรเจกต์ (Project Structure)](#-โครงสร้างโปรเจกต์-project-structure)
- [เอกสาร API (API Documentation)](#-เอกสาร-api-api-documentation)
- [สคริปต์และเครื่องมือ (Scripts & Utilities)](#-สคริปต์และเครื่องมือ-scripts--utilities)
- [การติดตั้งและตั้งค่า (Installation & Setup)](#-การติดตั้งและตั้งค่า-installation--setup)
- [การเชื่อมต่อกับ FiveM (FiveM Integration)](#-การเชื่อมต่อกับ-fivem-fivem-integration)
- [คำสั่งสำหรับการพัฒนา (Development Commands)](#-คำสั่งสำหรับการพัฒนา-development-commands)

---

## ✨ ฟีเจอร์และระบบหลัก (Key Features)

### 1. 🔐 ระบบสมาชิกและการยืนยันตัวตน (Authentication)
- **Discord Login:** ใช้ **NextAuth.js** เชื่อมต่อกับ Discord OAuth2 ทำให้ผู้เล่นสามารถล็อกอินด้วยบัญชี Discord ได้ทันที
- **Auto-Sync:** ดึงข้อมูลรูปโปรไฟล์, Username, และ Discord ID มาเก็บลงฐานข้อมูลอัตโนมัติ
- **Session Management:** จัดการ Session การใช้งานอย่างปลอดภัย

### 2. 📝 ระบบลงทะเบียนล่วงหน้า (Pre-registration)
- **Real-time Counter:** แสดงจำนวนผู้ลงทะเบียนทั้งหมดแบบ Real-time
- **Milestone System:** แถบความคืบหน้าของยอดลงทะเบียน พร้อมของรางวัลที่จะปลดล็อกเมื่อถึงเป้าหมาย
- **Recent Registrations:** Ticker แสดงรายชื่อผู้ที่เพิ่งลงทะเบียนล่าสุดแบบเลื่อนอัตโนมัติ
- **Unique ID Generation:** สร้างเลขบัตรประชาชน (SSN) และวันเกิดให้อัตโนมัติสำหรับตัวละครในเกม

### 3. 🎰 ระบบสุ่มรางวัล (Lucky Draw / Gachapon)
- **Server-Side Logic:** การคำนวณเกลือ (RNG) เกิดขึ้นที่ฝั่ง Server 100% เพื่อป้องกันการโกง
- **Weighted Rates:** รองรับการตั้งค่าเรทออกของไอเทมตามน้ำหนัก (Common, Rare, Epic, Legendary)
- **Premium Animation:**
    - เอฟเฟกต์หมุนวงล้อแบบ Tape-roll
    - แสง Sunburst และ Particle ตามระดับความหายาก
    - เสียงประกอบเมื่อหมุนและได้รับของรางวัล
- **History:** บันทึกประวัติการสุ่มของผู้เล่นทุกคน

### 4. 📅 ระบบเช็คชื่อรายวัน (Daily Check-in)
- **7-Day Cycle:** ระบบเช็คชื่อต่อเนื่อง 7 วัน พร้อมของรางวัลที่แตกต่างกันในแต่ละวัน
- **Streak Tracking:** ตรวจสอบการเช็คชื่อต่อเนื่อง หากขาดไปจะเริ่มนับใหม่ (Configurable)
- **Status Validation:** ตรวจสอบสถานะการรับของรางวัลในแต่ละวัน (รับแล้ว/ยังไม่รับ/เลยกำหนด)

### 5. 👥 ระบบแก๊ง (Gang System)
- **Create Gang:** ผู้เล่นสามารถสร้างแก๊งของตัวเองได้ (อาจมีเงื่อนไขค่าใช้จ่าย)
- **Invite System:** ระบบสร้างลิงก์เชิญเพื่อนเข้าแก๊ง
- **Member Management:** หัวหน้าแก๊งสามารถดูรายชื่อสมาชิกในแก๊งได้
- **Gang Stats:** แสดงสถิติของแก๊ง เช่น จำนวนสมาชิก, เลเวลแก๊ง

### 6. 📰 ระบบข่าวสาร (News & Updates)
- **Categories:** แยกหมวดหมู่ข่าวสาร (ประกาศ, กิจกรรม, อัปเดต, โปรโมชั่น)
- **Featured News:** ปักหมุดข่าวสำคัญไว้ด้านบนสุด
- **Pagination:** รองรับการแบ่งหน้าข่าวสารหากมีจำนวนมาก

### 7. 🛠️ ระบบหลังบ้าน (Admin Panel)
- **Dashboard:** ดูภาพรวมสถิติของเว็บไซต์ (ยอดคนลงทะเบียน, ยอดคนเข้าเว็บ)
- **Settings:** ตั้งค่าระบบต่างๆ ของเว็บไซต์ผ่านหน้าเว็บได้ทันที (เปิด/ปิดระบบลงทะเบียน, แก้ไขข้อความประกาศ)

---

## 🏗️ โครงสร้างทางเทคนิค (Technical Architecture)

### Tech Stack
- **Frontend Framework:** [Next.js 14](https://nextjs.org/) (App Router) - ใช้ฟีเจอร์ล่าสุดอย่าง Server Components และ Server Actions
- **Styling:** [Tailwind CSS](https://tailwindcss.com/) - Utility-first CSS framework
- **UI Components:** [Shadcn UI](https://ui.shadcn.com/) - Reusable components ที่สร้างบน Radix UI
- **Animations:** [Framer Motion](https://www.framer.com/motion/) - Library สำหรับทำ Animation ที่ซับซ้อน
- **Database:** MySQL - ฐานข้อมูลหลัก (ใช้ร่วมกับ Server FiveM ได้)
- **DB Client:** `mysql2` - Driver สำหรับเชื่อมต่อ MySQL ที่รองรับ Promise
- **Authentication:** [NextAuth.js](https://next-auth.js.org/) - จัดการระบบล็อกอิน
- **Icons:** [Lucide React](https://lucide.dev/) - ชุดไอคอนที่สวยงามและเบา

### Database Schema (โดยสังเขป)
- `users`: เก็บข้อมูลผู้เล่น (Discord ID, ชื่อ, รูปภาพ, สถานะการลงทะเบียน)
- `users_preregister`: เก็บข้อมูลการลงทะเบียนล่วงหน้า
- `lucky_draw_history`: ประวัติการสุ่มรางวัล
- `daily_checkin`: ประวัติการเช็คชื่อ
- `gangs`: ข้อมูลแก๊ง
- `gang_members`: ความสัมพันธ์สมาชิกในแก๊ง

---

## 📂 โครงสร้างโปรเจกต์ (Project Structure)

```
rank1-city-web/
├── app/                    # Next.js App Router
│   ├── api/                # API Endpoints (Backend Logic)
│   ├── admin/              # หน้า Admin Panel
│   ├── auth/               # หน้า Login/Error
│   ├── news/               # หน้าข่าวสาร
│   ├── register/           # หน้าลงทะเบียน
│   ├── layout.js           # Root Layout (Navbar, Footer, Providers)
│   └── page.js             # หน้า Home (Landing Page)
├── components/             # React Components
│   ├── ui/                 # Shadcn UI Components (Button, Input, etc.)
│   ├── FeatureTabs.js      # แท็บแสดงฟีเจอร์หน้าแรก
│   ├── LuckyDraw.js        # คอมโพเนนต์ระบบสุ่มรางวัล
│   └── ...
├── lib/                    # Utilities & Libraries
│   ├── db.js               # เชื่อมต่อ Database
│   └── utils.js            # Helper functions
├── public/                 # Static Files (Images, Fonts)
├── scripts/                # Node.js Scripts สำหรับจัดการระบบ
└── styles/                 # Global Styles
```

---

## 🔌 เอกสาร API (API Documentation)

API ทั้งหมดจะอยู่ที่ `/api/*` และส่งข้อมูลกลับในรูปแบบ JSON

| Endpoint | Method | คำอธิบาย |
|----------|--------|----------|
| `/api/auth/[...nextauth]` | GET/POST | จัดการระบบ Login/Logout ของ NextAuth |
| `/api/user/check` | GET | ตรวจสอบสถานะ User ปัจจุบัน (ล็อกอินหรือยัง, ลงทะเบียนหรือยัง) |
| `/api/user/register` | POST | ส่งข้อมูลลงทะเบียนผู้เล่นใหม่ |
| `/api/user/profile` | GET | ดึงข้อมูลโปรไฟล์ผู้ใช้ |
| `/api/preregister` | GET | ดึงข้อมูลสถานะการลงทะเบียนล่วงหน้า (ยอดรวม, เป้าหมาย) |
| `/api/preregister/recent` | GET | ดึงรายชื่อผู้ลงทะเบียนล่าสุด 10-20 คน |
| `/api/luckydraw` | POST | สุ่มรางวัล (Spin) |
| `/api/luckydraw` | GET | ดึงข้อมูลไอเทมในตู้และประวัติการสุ่ม |
| `/api/checkin` | GET/POST | ดึงข้อมูลเช็คชื่อ / กดเช็คชื่อ |
| `/api/gang` | GET/POST | จัดการแก๊ง (สร้าง, ดูข้อมูล) |
| `/api/news` | GET | ดึงรายการข่าวสาร |
| `/api/admin/settings` | GET/POST | จัดการตั้งค่าเว็บไซต์ (Admin Only) |

---

## 🛠️ สคริปต์และเครื่องมือ (Scripts & Utilities)

ในโฟลเดอร์ `scripts/` มีเครื่องมือสำหรับจัดการระบบและฐานข้อมูล:

1.  **`simulate_registrations.js`**
    -   **หน้าที่:** จำลองการลงทะเบียนผู้เล่นปลอม (Bot) เพื่อทดสอบระบบ Real-time counter และ Ticker
    -   **วิธีใช้:** `node scripts/simulate_registrations.js`

2.  **`init-preregister-db.js`**
    -   **หน้าที่:** สร้างตาราง Database ที่จำเป็นสำหรับระบบ Pre-register ทั้งหมด
    -   **วิธีใช้:** `node scripts/init-preregister-db.js`

3.  **`add_tickets.js` / `give-tickets.js`**
    -   **หน้าที่:** เพิ่มตั๋วสุ่มรางวัลให้กับผู้เล่น (สำหรับแจกรางวัลชดเชยหรือกิจกรรม)
    -   **วิธีใช้:** แก้ไข ID ในไฟล์แล้วรัน `node scripts/give-tickets.js`

4.  **`check-db.js`**
    -   **หน้าที่:** ตรวจสอบความถูกต้องของข้อมูลใน Database และ Connection
    -   **วิธีใช้:** `node scripts/check-db.js`

5.  **`update_prereg_schema.js`**
    -   **หน้าที่:** อัปเดตโครงสร้างตาราง (Schema Migration) เมื่อมีการเปลี่ยนแปลงเวอร์ชัน
    -   **วิธีใช้:** `node scripts/update_prereg_schema.js`

---

## 🚀 การติดตั้งและตั้งค่า (Installation & Setup)

### 1. สิ่งที่ต้องมี (Prerequisites)
- Node.js (v18 ขึ้นไป)
- MySQL Database (หรือ MariaDB)
- Discord Developer Application (สำหรับ Login)

### 2. ติดตั้งโปรเจกต์
```bash
git clone https://github.com/TRPxx/rank1-city-web.git
cd rank1-city-web
npm install
```

### 3. ตั้งค่า Environment Variables
สร้างไฟล์ `.env` ที่ root folder และใส่ค่าดังนี้:

```env
# Database Connection
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=es_extended  # ชื่อ Database ของเซิร์ฟเวอร์เกม

# NextAuth (Discord Login)
DISCORD_CLIENT_ID=your_discord_client_id
DISCORD_CLIENT_SECRET=your_discord_client_secret
NEXTAUTH_SECRET=your_random_secret_string
NEXTAUTH_URL=http://localhost:3000

# App Settings
NEXT_PUBLIC_API_URL=http://localhost:3000
```

### 4. เตรียมฐานข้อมูล
รันคำสั่งเพื่อสร้างตารางที่จำเป็น:
```bash
node scripts/init-preregister-db.js
```

### 5. รันเซิร์ฟเวอร์
```bash
npm run dev
```
เข้าใช้งานได้ที่ `http://localhost:3000`

---

## 🎮 การเชื่อมต่อกับ FiveM (FiveM Integration)

ระบบนี้ออกแบบมาให้ทำงานร่วมกับเซิร์ฟเวอร์ **ESX Legacy** โดยมีหลักการทำงานดังนี้:

### Phase 1: Web Registration
1.  ผู้เล่นล็อกอิน Discord บนเว็บ
2.  กรอกข้อมูลลงทะเบียน (ชื่อ IC, วันเกิด ฯลฯ)
3.  ระบบบันทึกข้อมูลลงตาราง `users` โดยใช้ `web_pending:discord_id` เป็น identifier ชั่วคราว

### Phase 2: FiveM Server Check (Lua Script)
ต้องมีการแก้ไขสคริปต์ `esx_identity` หรือตัวจัดการการเชื่อมต่อ:
1.  เมื่อผู้เล่นเข้าเกม (`playerConnecting`)
2.  Server ดึง Discord ID ของผู้เล่น
3.  ตรวจสอบใน Database:
    -   **ถ้ามีข้อมูล (ลงทะเบียนแล้ว):** อัปเดต identifier จาก `web_pending:...` เป็น License จริงของผู้เล่น (`license:...`) และอนุญาตให้เข้าเกม
    -   **ถ้าไม่มีข้อมูล:** เตะผู้เล่น (DropPlayer) พร้อมข้อความแจ้งให้ไปลงทะเบียนที่เว็บไซต์

### 📜 สคริปต์เสริม (Included Scripts)
ในโฟลเดอร์ `fivem-scripts/` จะมีไฟล์ `sv_claim.lua` สำหรับใส่ในเซิร์ฟเวอร์ FiveM:
- **Command:** `/redeem`
- **หน้าที่:** ตรวจสอบของรางวัลที่ค้างอยู่ในเว็บ (เช่น จาก Lucky Draw หรือ Check-in) และเสกของเข้าตัวผู้เล่นทันที
- **การติดตั้ง:** นำโค้ดไปใส่ใน Resource ใดก็ได้ (ฝั่ง Server) และแก้ไข `WEB_API_URL` ให้ตรงกับเว็บ

---

## 💻 คำสั่งสำหรับการพัฒนา (Development Commands)

- `npm run dev`: รันเซิร์ฟเวอร์ในโหมดพัฒนา (Hot Reload)
- `npm run build`: สร้าง Production Build
- `npm start`: รันเซิร์ฟเวอร์ Production
- `npm run lint`: ตรวจสอบ Code Quality
- `npm test`: รัน Unit Tests

---

**Developed with ❤️ by Rank1 Development Team**
