# 📋 รายการสิ่งที่ต้องทำ (Audit & Optimization Checklist)

เอกสารนี้รวบรวมรายการสิ่งที่ต้องปรับปรุงเว็บไซต์ **Rank1 City** เพื่อให้ได้คะแนน Performance, Accessibility, SEO และ Best Practices สูงสุด (100/100) ตามมาตรฐาน Google Lighthouse

---

## 🚀 1. Performance (ประสิทธิภาพ)
> **เป้าหมาย:** โหลดหน้าเว็บให้เร็วที่สุด (LCP < 2.5s) และลดการกระตุก (CLS < 0.1)

- [x] **(P0) เปลี่ยนไฟล์ภาพพื้นหลังเป็น WebP/AVIF**
  - **ปัญหา:** ไฟล์ `public/images/hero-bg-fivem.png` เป็น PNG ซึ่งมีขนาดใหญ่ ทำให้โหลดหน้าแรกช้า
  - **วิธีแก้:** แปลงไฟล์เป็น `.webp` หรือ `.avif` แล้วอัปเดตโค้ดใน `app/page.js`
  - **ไฟล์ที่เกี่ยวข้อง:** `app/page.js`, `public/images/`

- [x] **(P1) ตรวจสอบการใช้ Animation (Framer Motion)**
  - **ปัญหา:** การใช้ Animation เยอะเกินไปอาจทำให้มือถือรุ่นเก่ากระตุก (High TBT)
  - **วิธีแก้:** ใช้ `will-change` ใน CSS สำหรับ element ที่มีการขยับเยอะๆ และพิจารณาลด Animation ในโหมด Mobile
  - **ไฟล์ที่เกี่ยวข้อง:** `app/page.js`, `components/LuckyDraw.js`

---

## ♿ 2. Accessibility (การเข้าถึง)
> **เป้าหมาย:** ให้ทุกคนใช้งานเว็บได้สะดวก รวมถึงผู้พิการทางสายตา

- [x] **(P1) ตรวจสอบ Contrast ของสีตัวอักษร**
  - **ปัญหา:** สี `text-muted-foreground` (สีเทา) บนพื้นหลังสีดำ อาจจะอ่านยากสำหรับบางคน (Contrast Ratio ต่ำกว่า 4.5:1)
  - **วิธีแก้:** ปรับเฉดสีเทาใน `globals.css` ให้สว่างขึ้นเล็กน้อยใน Dark Mode
  - **ไฟล์ที่เกี่ยวข้อง:** `app/globals.css`

- [x] **(P2) เพิ่ม Focus Ring ให้ปุ่มกด**
  - **ปัญหา:** ผู้ใช้ที่ใช้คีย์บอร์ด (Tab) อาจมองไม่เห็นว่ากำลังเลือกปุ่มไหนอยู่
  - **วิธีแก้:** ตรวจสอบว่าปุ่มทุกปุ่มมี state `focus-visible` ที่ชัดเจน (เช่น มีกรอบสีฟ้าขึ้น)
  - **ไฟล์ที่เกี่ยวข้อง:** `components/ui/button.js`

---

## 🔍 3. SEO (การค้นหาบน Google)
> **เป้าหมาย:** ติดหน้าแรก Google และแสดงผลสวยงามบน Social Media

- [x] **(P0) เพิ่ม `metadataBase`**
  - **ปัญหา:** รูปภาพ Preview เวลาแชร์ลง Facebook/Discord อาจไม่ขึ้น เพราะ URL เป็นแบบ Relative
  - **วิธีแก้:** เพิ่ม `metadataBase: new URL('https://rank1city.com')` ในไฟล์ layout
  - **ไฟล์ที่เกี่ยวข้อง:** `app/layout.js`

- [x] **(P2) เพิ่ม Structured Data (JSON-LD)**
  - **ปัญหา:** Google ยังไม่รู้ว่าเว็บนี้คือ "เกมเซิร์ฟเวอร์"
  - **วิธีแก้:** เพิ่ม Script JSON-LD เพื่อระบุว่าเป็น `VideoGame` หรือ `Organization`
  - **ไฟล์ที่เกี่ยวข้อง:** `app/page.js`

---

## 📱 4. PWA & Mobile Experience
> **เป้าหมาย:** ให้เว็บใช้งานได้เหมือนแอปพลิเคชัน

- [x] **(P1) สร้าง Web App Manifest**
  - **ปัญหา:** เว็บยังติดตั้งลงเครื่อง (Install to Home Screen) ไม่ได้
  - **วิธีแก้:** สร้างไฟล์ `app/manifest.js` หรือ `public/manifest.json` ระบุชื่อแอป, ไอคอน และสีธีม
  - **ไฟล์ที่เกี่ยวข้อง:** `app/manifest.js`

- [x] **(P1) ปิด Scroll Snap บนมือถือ**
  - **ปัญหา:** ในหน้าจอเล็กๆ ถ้ารายละเอียดเยอะ `snap-y` จะทำให้เลื่อนดูเนื้อหาด้านล่างสุดไม่ได้ (ติดขอบ)
  - **วิธีแก้:** เปลี่ยน class `snap-y` เป็น `md:snap-y` (ให้ Snap เฉพาะจอ Tablet ขึ้นไป)
  - **ไฟล์ที่เกี่ยวข้อง:** `app/page.js`

---

## 🛠️ 5. Code Quality & Cleanup
> **เป้าหมาย:** โค้ดสะอาด ดูแลรักษาง่าย

- [x] **(P2) ลบ `suppressHydrationWarning` ถ้าไม่จำเป็น**
  - **ปัญหา:** การใส่ไว้ตลอดเวลาอาจซ่อนปัญหาที่แท้จริงของการ Render
  - **วิธีแก้:** ลองลบออกแล้วดูว่ามี Error ใน Console หรือไม่ ถ้าไม่มีให้ลบถาวร
  - **ไฟล์ที่เกี่ยวข้อง:** `app/layout.js`

---

### 📌 สรุปสิ่งที่ต้องทำก่อน (Priority High)
1.  แปลงรูป Hero เป็น WebP
2.  เพิ่ม `metadataBase`
3.  สร้างไฟล์ Manifest (PWA)
4.  แก้ Scroll Snap บนมือถือ
