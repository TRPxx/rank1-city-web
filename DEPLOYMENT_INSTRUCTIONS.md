# 🚀 Deployment Instructions (สำหรับคุณบอม)

โค้ดทั้งหมดถูกอัปเดตขึ้น Git เรียบร้อยแล้วครับ พร้อมสำหรับการ Deploy ขึ้น Production

## 1. อัปเดตโค้ด (Update Code)
```bash
git pull origin main
npm install
npm run build
```

## 2. อัปเดตฐานข้อมูล (Database Update) **สำคัญมาก**
กรุณารันไฟล์ SQL ต่อไปนี้ใน Database `rank1city_web` (เรียงตามลำดับ):

1.  **`update_schema_v2.sql`**
    *   *สิ่งที่ทำ:* สร้างตาราง `transaction_logs` และ `families`
    *   *หมายเหตุ:* ถ้ารันไปแล้วข้ามได้เลย

2.  **`add_indexes_v3.sql`** (ไฟล์ใหม่ ⚡)
    *   *สิ่งที่ทำ:* เพิ่ม Index ให้กับตารางต่างๆ เพื่อให้ระบบค้นหาเร็วขึ้น และรองรับคน 5,000 คน
    *   *ต้องรัน:* **ห้ามลืมเด็ดขาด** ไม่งั้นหน้า Admin จะช้ามาก

## 3. เริ่มระบบใหม่ (Restart Server)
```bash
# Restart Next.js server
pm2 restart rank1-web
# หรือ
npm run start
```

---
*อัปเดตล่าสุด: 4 ธันวาคม 2025*
