# 🏙️ Rank1 City - Project Documentation

**Rank1 City Website** คือแพลตฟอร์มเว็บแอปพลิเคชันครบวงจรสำหรับเซิร์ฟเวอร์ FiveM Roleplay ระดับพรีเมียม ที่ถูกออกแบบมาเพื่อเชื่อมต่อโลกของเกมเข้ากับเว็บไซต์อย่างสมบูรณ์แบบ รองรับระบบสมาชิก, กิจกรรม, ระบบสุ่มของรางวัล และการจัดการภายในเซิร์ฟเวอร์

---

## 🛠️ Tech Stack (เทคโนโลยีที่ใช้)

### Frontend
- **Framework:** [Next.js 14](https://nextjs.org/) (App Router) - ใช้สถาปัตยกรรมล่าสุดเน้น Server Components และ Server Actions
- **Language:** JavaScript (ES6+)
- **Styling:** [Tailwind CSS](https://tailwindcss.com/) - Utility-first CSS framework
- **UI Components:** [Shadcn UI](https://ui.shadcn.com/) - ชุด Components ที่มีความยืดหยุ่นสูง (Built on Radix UI)
- **Animations:** [Framer Motion](https://www.framer.com/motion/) - สำหรับทำ Animation ที่ลื่นไหลและซับซ้อน
- **Icons:** [Lucide React](https://lucide.dev/)

### Backend & Database
- **Runtime:** Node.js
- **Database:** MySQL (ใช้งานร่วมกับฐานข้อมูลของเซิร์ฟเวอร์ FiveM)
- **ORM/Query Builder:** `mysql2` (ใช้ Raw SQL หรือ Query Builder แบบ Lightweight)
- **Authentication:** [NextAuth.js](https://next-auth.js.org/) (Discord Provider)

### Tools & DevOps
- **Package Manager:** npm
- **Version Control:** Git
- **Scripts:** Node.js scripts สำหรับจัดการ Database และจำลองข้อมูล

---

## ✨ Features (ฟีเจอร์หลัก)

### 1. 🔐 Authentication (ระบบสมาชิก)
- **Discord Login:** ผู้เล่นล็อกอินผ่าน Discord Account (OAuth2)
- **Auto Sync:** ระบบดึงข้อมูลรูปโปรไฟล์, Username, และ Discord ID อัตโนมัติ
- **Session Management:** จัดการสถานะการล็อกอินอย่างปลอดภัย

### 2. 📝 Pre-registration (ระบบลงทะเบียนล่วงหน้า)
- **Real-time Counter:** แสดงยอดผู้ลงทะเบียนทั้งหมดแบบ Real-time
- **Milestone System:** แถบความคืบหน้าของยอดลงทะเบียน ปลดล็อกของรางวัลตามเป้าหมาย
- **Recent Ticker:** แถบแสดงรายชื่อผู้ที่เพิ่งลงทะเบียนล่าสุดแบบเลื่อนอัตโนมัติ
- **Unique ID (SSN):** ระบบสร้างเลขบัตรประชาชนและวันเกิดให้ตัวละครในเกมอัตโนมัติ

### 3. 🎰 Lucky Draw (ระบบกาชาปอง)
- **Server-Side RNG:** การคำนวณผลลัพธ์เกิดขึ้นที่ Server 100% เพื่อความปลอดภัย
- **Weighted Rates:** กำหนดอัตราการออกของไอเทมตามระดับความหายาก (Common, Rare, Epic, Legendary)
- **Visual Effects:**
    - Animation การหมุนแบบ Tape-roll
    - Effect แสงและ Particle ตามระดับความหายากของไอเทม
- **History:** ผู้เล่นสามารถดูประวัติการสุ่มของตัวเองได้

### 4. 📅 Daily Check-in (ระบบเช็คชื่อรายวัน)
- **7-Day Cycle:** รอบการเช็คชื่อ 7 วัน พร้อมของรางวัลที่กำหนดเองได้
- **Streak System:** ระบบนับจำนวนวันที่เช็คชื่อต่อเนื่อง
- **Status Tracking:** ตรวจสอบสถานะรายวัน (รับแล้ว, ยังไม่รับ, ข้าม)

### 5. 👥 Gang System (ระบบแก๊ง)
- **Management:** สร้างแก๊ง, เชิญสมาชิก, และจัดการสมาชิกในแก๊ง
- **Dashboard:** ดูสถิติของแก๊งและรายชื่อสมาชิกทั้งหมด

### 6. 📰 News & Updates (ระบบข่าวสาร)
- **Categorization:** แบ่งหมวดหมู่ข่าว (ประกาศ, กิจกรรม, โปรโมชั่น)
- **Featured News:** ระบบปักหมุดข่าวสำคัญ
- **Pagination:** รองรับการแสดงผลข่าวจำนวนมาก

### 7. 🛠️ Admin Panel (ระบบหลังบ้าน)
- **Dashboard:** ดูสถิติรวมของเว็บไซต์ (ยอด User, ยอดลงทะเบียน)
- **Settings:** เปิด/ปิด ระบบต่างๆ ของเว็บไซต์ได้ทันที (เช่น ปิดรับลงทะเบียน, ปิดระบบเติมเงิน)
- **Management:** จัดการข้อมูลผู้เล่นและตั้งค่า Config ต่างๆ

---

## 📂 Project Structure (โครงสร้างโปรเจกต์)

```
rank1-city-web/
├── app/                        # Next.js App Router (Main Logic)
│   ├── admin/                  # หน้า Admin Panel และ Settings
│   ├── api/                    # Backend API Endpoints
│   │   ├── auth/               # NextAuth Handlers
│   │   ├── admin/              # Admin APIs
│   │   ├── luckydraw/          # Lucky Draw Logic
│   │   ├── user/               # User Data APIs
│   │   └── ...
│   ├── news/                   # หน้าแสดงข่าวสาร
│   ├── register/               # หน้าลงทะเบียน
│   ├── layout.js               # Root Layout (Navbar, Footer, Providers)
│   └── page.js                 # Landing Page (Home)
├── components/                 # React Components
│   ├── admin/                  # Components สำหรับ Admin
│   ├── ui/                     # Shadcn UI Base Components (Button, Input, etc.)
│   ├── LuckyDraw.js            # Logic และ UI ของระบบสุ่มรางวัล
│   ├── DailyCheckIn.js         # Logic และ UI ของระบบเช็คชื่อ
│   ├── Navbar.js               # เมนูนำทางหลัก
│   └── ...
├── lib/                        # Utilities
│   ├── db.js                   # Database Connection Pool
│   └── utils.js                # Helper Functions (Formatters, Validators)
├── public/                     # Static Assets
│   ├── images/                 # รูปภาพประกอบเว็บไซต์
│   └── fonts/                  # ไฟล์ฟอนต์
├── scripts/                    # Utility Scripts (Run via Node.js)
│   ├── init-preregister-db.js  # สร้างตาราง Database เริ่มต้น
│   ├── simulate_registrations.js # จำลองข้อมูลเพื่อ Test
│   └── ...
└── styles/                     # CSS Files
    └── globals.css             # Global Styles & Tailwind Directives
```

---

## 🗄️ Database Schema (โครงสร้างฐานข้อมูล)

โปรเจกต์นี้ใช้ MySQL โดยมีตารางหลักๆ ดังนี้:

| Table Name | Description |
|------------|-------------|
| `users` | เก็บข้อมูลหลักของผู้เล่น (Discord ID, Name, Avatar) |
| `users_preregister` | เก็บข้อมูลการลงทะเบียนล่วงหน้า (SSN, DOB, Status) |
| `lucky_draw_history` | ประวัติการสุ่มรางวัลของผู้เล่น |
| `daily_checkin` | ประวัติการเช็คชื่อรายวัน |
| `gangs` | ข้อมูลแก๊ง (ชื่อ, เลเวล, หัวหน้าแก๊ง) |
| `gang_members` | ข้อมูลสมาชิกในแก๊งและความสัมพันธ์ |
| `web_settings` | การตั้งค่าต่างๆ ของเว็บไซต์ (Admin Settings) |

---

## 🔌 FiveM Integration (การเชื่อมต่อกับเกม)

ระบบเว็บไซต์ทำงานร่วมกับเซิร์ฟเวอร์ FiveM (ESX Legacy) ผ่านฐานข้อมูลเดียวกัน:

1.  **Registration:** เมื่อผู้เล่นลงทะเบียนบนเว็บ ข้อมูลจะถูกบันทึกพร้อมสถานะ `pending` หรือสร้าง Identifier ชั่วคราว
2.  **Connection:** เมื่อผู้เล่นเข้าเกม Script ฝั่ง Server (Lua) จะตรวจสอบ Discord ID กับฐานข้อมูล
    - **Found:** อัปเดต License และอนุญาตให้เข้าเกม
    - **Not Found:** เตะผู้เล่นออกพร้อมแจ้งให้ไปลงทะเบียน
3.  **Item Claiming:** ระบบ `sv_claim.lua` (ใน `fivem-scripts/`) ใช้สำหรับเสกของรางวัลที่ผู้เล่นได้รับจากกิจกรรมบนเว็บเข้าตัวละครในเกม

---

## 🚀 Getting Started (การติดตั้งและใช้งาน)

1.  **Clone Repository:**
    ```bash
    git clone <repo_url>
    ```
2.  **Install Dependencies:**
    ```bash
    npm install
    ```
3.  **Setup Environment:**
    - สร้างไฟล์ `.env` และระบุค่า `DB_HOST`, `DB_USER`, `DB_PASSWORD`, `DISCORD_CLIENT_ID`, `DISCORD_CLIENT_SECRET` ฯลฯ
4.  **Initialize Database:**
    ```bash
    node scripts/init-preregister-db.js
    ```
5.  **Run Development Server:**
    ```bash
    npm run dev
    ```
    เข้าใช้งานได้ที่ `http://localhost:3000`
