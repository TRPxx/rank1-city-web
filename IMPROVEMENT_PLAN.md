# 📋 แผนการปรับปรุงระบบ Rank1 City (Improvement Plan)

เอกสารนี้รวบรวมรายการสิ่งที่ต้องปรับปรุงทั้งหมดตามผลการทดสอบจำลองผู้ใช้งาน 5,000 คน โดยเรียงลำดับความสำคัญจาก **ด่วนที่สุด** ไปหา **สิ่งที่ควรทำเพิ่ม**

---

## 🚨 1. สิ่งที่ต้องทำทันที (Critical / Must Fix)
*หากไม่ทำ ระบบจะมีปัญหาข้อมูลไม่ครบถ้วนและตรวจสอบยาก*

### 1.1 📝 บันทึก Lucky Draw ลง Transaction Logs (P0)
*   **ปัญหา:** ปัจจุบันเมื่อผู้เล่นสุ่มกาชา ระบบหักตั๋วในตาราง `preregistrations` แต่ **ไม่ได้บันทึก** ลงใน `transaction_logs` ทำให้ Admin ไม่เห็นเส้นทางการใช้ตั๋วในหน้า Economy
*   **สิ่งที่ต้องทำ:**
    *   แก้ไขไฟล์ `app/api/luckydraw/route.js`
    *   เพิ่มคำสั่ง SQL `INSERT INTO transaction_logs` ภายใน Transaction เดียวกันกับการหักตั๋ว
    *   **ข้อมูลที่ต้องบันทึก:**
        *   `action`: 'lucky_draw_spin'
        *   `amount`: -1
        *   `details`: 'Won [Item Name]'

### 1.2 🗄️ เพิ่ม Database Indexes (P1)
*   **ปัญหา:** การค้นหาในหน้า Admin (เช่น หาชื่อคน, หาชื่อไอเทม) จะช้ามากเมื่อมีข้อมูลหลักหมื่น/แสนรายการ เพราะระบบต้องกวาดข้อมูลทั้งตาราง (Full Table Scan)
*   **สิ่งที่ต้องทำ:** รันคำสั่ง SQL เพื่อสร้าง Index:
    ```sql
    -- สำหรับค้นหาประวัติการสุ่มด้วยชื่อไอเทม
    CREATE INDEX idx_lucky_draw_item_name ON lucky_draw_history(item_name);
    
    -- สำหรับค้นหาผู้ใช้ด้วยชื่อ Discord (ถ้าตาราง preregistrations ยังไม่มี)
    CREATE INDEX idx_prereg_discord_name ON preregistrations(discord_name);
    
    -- สำหรับกรองประเภทธุรกรรม
    CREATE INDEX idx_trans_action ON transaction_logs(action);
    ```

---

## 🚀 2. การปรับปรุงประสิทธิภาพ (Performance Optimization)
*ทำเพื่อให้ระบบรองรับคน 5,000+ ได้ลื่นไหล ไม่หน่วง*

### 2.1 ⚡ Cache Admin Stats (P2)
*   **ปัญหา:** หน้า Dashboard "ภาพรวม (Overview)" คำนวณยอดรวม (`COUNT(*)`) ทุกครั้งที่มีคนเปิดหน้า Admin ซึ่งกินทรัพยากร Database โดยไม่จำเป็น
*   **สิ่งที่ต้องทำ:**
    *   ใช้ `unstable_cache` ของ Next.js หรือสร้างตัวแปร Global ง่ายๆ ใน Memory เพื่อเก็บค่า Stats
    *   ตั้งเวลาอัปเดต (Revalidate) ทุกๆ 60 วินาที หรือ 5 นาที ก็เพียงพอสำหรับภาพรวม

### 2.2 🔍 ปรับปรุงการค้นหา (Search Optimization) (P2)
*   **ปัญหา:** การค้นหาด้วย `LIKE '%keyword%'` อาจจะยังช้าถ้าข้อมูลเยอะมากๆ
*   **สิ่งที่ต้องทำ:**
    *   จำกัดการค้นหาให้ต้องพิมพ์อย่างน้อย 3 ตัวอักษร
    *   พิจารณาใช้ Full-Text Search ของ MySQL หากต้องการค้นหาที่ฉลาดขึ้นในอนาคต

---

## 🎨 3. การปรับปรุงประสบการณ์ใช้งาน (UX/UI Enhancements)
*ทำเพื่อให้ Admin และผู้เล่นใช้งานได้สะดวกขึ้น*

### 3.1 📅 เพิ่มตัวกรองวันที่ (Date Range Filter) (P3)
*   **ปัญหา:** Admin ไม่สามารถดูข้อมูลเฉพาะช่วงเวลาได้ (เช่น อยากดูยอดสุ่มของ "เมื่อวาน" เท่านั้น)
*   **สิ่งที่ต้องทำ:**
    *   เพิ่ม Date Picker ในหน้า Winners และ Economy
    *   ส่งค่า `startDate` และ `endDate` ไปที่ API เพื่อกรองข้อมูล

### 3.2 ⏳ Loading Skeletons (P3)
*   **ปัญหา:** เวลาเปลี่ยนแท็บใน Admin หน้าจอจะกระตุกหรือโล่งไปชั่วขณะ
*   **สิ่งที่ต้องทำ:**
    *   เพิ่ม Component `Skeleton` (กรอบสีเทาๆ กระพริบ) ระหว่างรอโหลดข้อมูลในแต่ละแท็บ

### 3.3 🔔 Toast Notifications (P3)
*   **ปัญหา:** บางครั้ง Admin ทำรายการสำเร็จแต่ไม่มีอะไรแจ้งเตือน
*   **สิ่งที่ต้องทำ:**
    *   เพิ่มระบบ Toast (แจ้งเตือนมุมขวาบน) เมื่อบันทึกข้อมูล, แบนผู้ใช้, หรือแก้ไขการตั้งค่าสำเร็จ

---

## 📋 สรุป Checklist สำหรับนักพัฒนา

- [ ] **Backend:** แก้ไข `luckydraw/route.js` เพิ่ม Insert Transaction Log
- [ ] **Database:** รัน SQL เพิ่ม Index 3 ตัว (`item_name`, `discord_name`, `action`)
- [ ] **Backend:** ทำ Caching ให้กับ API `/api/admin` ส่วนของ Stats
- [ ] **Frontend:** เพิ่มเงื่อนไขค้นหาต้องพิมพ์ 3 ตัวอักษรขึ้นไป
- [ ] **Frontend:** (Optional) เพิ่ม Date Picker ในหน้า Admin

---
*เอกสารนี้จัดทำขึ้นเมื่อ: 4 ธันวาคม 2025*
